- content_for :title do
  Adversaries

.row
  .small-12.columns
    %h1
      Adversaries
      - if can? :create, @adversary
        %small
          = link_to "<i class='fi-plus'></i>".html_safe, new_adversary_path
.row
  .small-12.columns

    %table.footable
      %thead
        %tr
          %th{data: {toggle: 'true', sort: {initial: "true"}}} Name
          %th{data: {hide: "all"}}
          %th Brawn
          %th Agility
          %th Intellect
          %th Cunning
          %th Willpower
          %th Presence
          %th Soak
          %th Defense ranged
          %th Defense melee
          %th Wounds
          %th Strain
          %th{data: {hide: "all"}} Race
          %th{data: {hide: "all"}} Abilities
          %th{data: {hide: "all"}}
          %th{data: {hide: "all"}} Book
          %th{colspan: 3, data: {hide: "phone,tablet", sort: {ignore: "true"}}}

      - @adversaries.each do |adversary|
        %tr
          %td= link_to adversary.name, adversary_path(adversary)
          %td= adversary.description
          %td= adversary.brawn
          %td= adversary.agility
          %td= adversary.intellect
          %td= adversary.cunning
          %td= adversary.willpower
          %td= adversary.presence
          %td= adversary.soak
          %td= adversary.defense_ranged
          %td= adversary.defense_melee
          %td= adversary.wounds
          %td= adversary.strain
          %td= adversary.race_id
          %td= adversary.abilities
          %td= image_tag(adversary.image_url)
          %td= "#{adversary.book.title} (p. #{adversary.page})" unless adversary.book.nil?
          %td{colspan: 3}
            = link_to 'Show', adversary
            = link_to 'Edit', edit_adversary_path(adversary)
            = link_to 'Destroy', adversary, :method => :delete, :data => { :confirm => 'Are you sure?' }
    .footable-pagination.hide-if-no-paging
      .pagination.pagination-centered
