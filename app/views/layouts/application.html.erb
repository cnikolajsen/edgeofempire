<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'images/apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'images/apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'images/apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'images/apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          
          <%= link_to "Edge of the Empire", root_path, class: "brand" %>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <% if user_signed_in? %>
              <li<% if current_page('races') %> class="active"<% end %>><%= link_to "Races", races_path  %></li>
              <li<% if current_page('careers') %> class="active"<% end %>><%= link_to "Careers", careers_path  %></li>
              <li<% if current_page('skills') %> class="active"<% end %>><%= link_to "Skills", skills_path  %></li>
              <li<% if current_page('talents') %> class="active"<% end %>><%= link_to "Talents", talents_path  %></li>
              <li class="dropdown <% if current_page('weapons') or current_page('armor') or current_page('gear') %>active<% end %>">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Equipment<b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li<% if current_page('weapons') %> class="active"<% end %>><%= link_to "Weapons", weapons_path %></li>
                  <li<% if current_page('armor') %> class="active"<% end %>><%= link_to "Armor", "/armor" %><ro/li>
                  <li<% if current_page('gear') %> class="active"<% end %>><%= link_to "Gear", "/gear"  %></li>
                </ul>
              </li>
              <li<% if current_page('obligations') %> class="active"<% end %>><%= link_to "Obligations", obligations_path  %></li>
              <li class="dropdown <% if current_page('human_names') %>active<% end %>">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Tools<b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li class="nav-header">Names</li>
                  <li<% if current_page('human_names') %> class="active"<% end %>><%= link_to "Human names", "/names/human" %></li>
                  <li<% if current_page('bothan_names') %> class="active"<% end %>><%= link_to "Bothan names", "/names/bothan" %></li>
                  <li<% if current_page('rodian_names') %> class="active"<% end %>><%= link_to "Rodian names", "/names/rodian" %></li>
                  <li<% if current_page('trandoshan_names') %> class="active"<% end %>><%= link_to "Trandoshan names", "/names/trandoshan"%></li>
                  <li<% if current_page('twilek_names') %> class="active"<% end %>><%= link_to "Twi'lek names", "/names/twilek" %></li>
                  <li<% if current_page('droid_names') %> class="active"<% end %>><%= link_to "Droid names", "/names/droid" %></li>
                  <li class="divider"></li>
                </ul>
              </li>
              
              <% else %>
                <li><%= link_to "Register", "/register" unless user_signed_in?  %></li>
                <li><%= link_to "Login", "/login" unless user_signed_in?  %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <% if user_signed_in? %>
          <div class="span3">
            <div class="well sidebar-nav">
              <ul class="nav nav-list">
                <li class="nav-header">You are logged in as:</li>
                <li><%= current_user.email %></li>
                <li<% if current_page('user') %> class="active"<% end %>><%= link_to "Edit profile", "/me" %></li>
                <li><%= link_to "Logout", "/logout" %></li>
                <li class="dropdown <% if current_page('characters') or current_page('new_character') %>active<% end %>">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    My characters
                    <b class="caret"></b>
                  </a>
                  <ul class="dropdown-menu">
                    <li<% if current_page('characters') %> class="active"<% end %>><%= link_to "List characters", characters_path()  %></li>
                    <li<% if current_page('new_character') %> class="active"<% end %>><%= link_to "Create character", new_character_path()  %></li>
                  </ul>
                </li>
              </ul>
            </div><!--/.well -->
            <%= image_tag("stormtrooper.jpg", :alt => "") %>
            
          </div><!--/span-->
        <% end %>
        <div class="span<%= user_signed_in? ? 9 : 12 %>">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->


    </div> <!-- /container -->
    <footer>
      <p>&copy; Gamer Geeks Galore 2013</p>
    </footer>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
