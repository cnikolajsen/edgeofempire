# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/


$('#free_career_skill_ranks').select2({
  maximumSelectionLength: $('#free_career_skill_ranks').attr('data-max')
});

$('#free_specialization_skill_ranks').select2({
  maximumSelectionLength: $('#free_specialization_skill_ranks').attr('data-max')
});

$('#free_non_career_skill_ranks').select2({
  maximumSelectionLength: $('#free_non_career_skill_ranks').attr('data-max')
});
